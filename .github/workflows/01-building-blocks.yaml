name: Building blocks

on: 
    #push
    workflow_dispatch:

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo "Hello World"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: |
                echo "I will Success"
                exit 0
            - name: Say Goodbye
              run: echo "Goodbye!"
