name: github context

on: push

jobs:
    echo-data:
        runs-on: ubuntu-latest
        steps:
            - name: dispaly information
              run: |
                echo "Event Name: ${{github.event_name}}"
                echo "Ref: ${{github.ref}}"
                echo "SHA: ${{github.sha}}"